# =============================================================
# == PRODUCTION ENVIRONMENT CONFIGURATION for React Build    ==
# =============================================================
# This file should be created at: /path/to/your/project/client/.env.production

# This tells the built React app that API requests should go to the same
# domain the website is on, but with the /api prefix.
# Example: If your site is at your_domain.com, API calls will go to your_domain.com/api
# Nginx will then proxy these requests to your backend on port 5000.
REACT_APP_API_URL=/api
NODE_ENV=production
DATABASE_URL=postgres://privuser:Aadhya%402050@localhost:5432/privcelebrations
SESSION_SECRET="Aadhya@2050"
PORT=3000
